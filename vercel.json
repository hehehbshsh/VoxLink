{
  "functions": {
    "api/websocket.py": {
      "memory": 512,
      "maxDuration": 60,
      "runtime": "python3.9" <--- Ensure you specify the Python runtime for Edge Functions
    }
  },
  "routes": [
    {
      "src": "/websocket",
      "dest": "/api/websocket.py"
    }
  ]
}

